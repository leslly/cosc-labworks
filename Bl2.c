#include <stdio.h>

int main()

/*persecent s means sob. thus leslie is equal to percent s
you can use as many as you want
it works for words only
percent d is for numbers only
numbers donot go inside the quotation marks
percent f is for numbers with decimal
point any value you put bfor percen f give the wanted number of s.f
*/
{
	
	printf ("%s is the best %s ever\n", "Leslie", "person");
	printf ("she ate %d bananas last night\n", 6);
	printf ("%f bananas got digested\n", 6.789);
	printf ("%.3f probability of pigs flying\n",3.45678);
}
